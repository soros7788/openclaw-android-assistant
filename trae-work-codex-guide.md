# 把 Trae Work 用出 Codex 效果的方法

核心思路：**不要只让 Trae 当补全工具，而是让它按"读仓库 → 改代码 → 跑测试 → 修 bug → 输出结果"的代理流程工作。**

## 使用 Work / Agent 模式

- 不要用普通 Chat 模式，尽量进入 Trae 的 `Work`、`Agent` 或"自动执行任务"模式。
- 让它可以读取项目文件、修改代码、运行命令。

## 给明确任务，而不是泛泛提问

- 不推荐：
  > "帮我看看这个项目"
- 推荐：
  > "请阅读当前仓库结构，找出登录功能的入口文件，修复登录失败时没有错误提示的问题，并运行相关测试。"

## 要求它按 Codex 式流程执行

可以这样写：

```text
请按以下步骤执行：
1. 先扫描项目结构，确认技术栈和入口文件。
2. 找到与问题相关的代码。
3. 修改最小必要文件。
4. 运行相关测试或构建命令。
5. 如果失败，继续修复。
6. 最后总结修改了哪些文件、为什么修改、如何验证。
```

## 明确授权它运行命令

例如：

```text
你可以运行必要的命令来定位问题，比如 rg、npm test、pnpm dev、pytest、cargo test。
修改文件前先说明计划，修改后运行验证命令。
```

## 要求它不要过度重构

这点很重要，否则 Trae 可能会顺手改很多无关代码。

```text
只修改与问题直接相关的文件，避免大规模重构。
如果不确定，请先询问我。
```

## 让它输出类似 Codex 的结果

可以要求：

```text
最后请输出：
- 修改的文件
- 修改原因
- 运行的命令
- 测试结果
- 是否还有风险
```

## 推荐 Prompt 模板

```text
请像 Codex 一样处理这个任务。

目标：修复当前项目的登录错误提示问题。

要求：
1. 先阅读项目结构和相关文件。
2. 定位登录逻辑和错误处理代码。
3. 只修改最小必要代码。
4. 修改后运行相关测试或构建命令。
5. 如果测试失败，请继续修复。
6. 最后总结：修改文件、原因、验证命令、测试结果、剩余风险。
```

## 简单总结

- 想让 `Trae Work` 接近 `Codex`：用 **Agent/Work 模式 + 明确任务 + 允许运行命令 + 要求测试验证**。  
- 关键不是功能本身，而是提示词要让它按"代理式开发流程"执行，而不是只生成代码片段。
